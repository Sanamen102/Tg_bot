# Скрипты и конфиги, исполняемые на Linux-сервере, всегда с LF —
# иначе bash/python на хосте спотыкаются о CRLF от Windows-разработки.
*.sh   text eol=lf
*.py   text eol=lf
Dockerfile text eol=lf
*.yml  text eol=lf
*.yaml text eol=lf
